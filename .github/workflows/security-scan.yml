name: Security Scanning

on:
  push:
    branches: [ master, develop, 'feat/**', 'refactor/**' ]
  pull_request:
    branches: [ master, develop ]
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:

jobs:
  security-scan:
    name: Run Security Checks
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
      pull-requests: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    
    - name: Set up Python 3.11
      uses: actions/setup-python@v6
      with:
        python-version: '3.11'
        cache: 'pip'
        
    - name: Install security tools
      run: |
        pip install --upgrade pip
        pip install bandit pip-audit
        
    - name: Run Bandit (Python Security Linter)
      run: |
        bandit -r services/ app/src/ \
          --exclude '**/tests/**,**/__pycache__/**' \
          --severity-level high \
          --confidence-level medium \
          -f json -o bandit-report.json
        bandit -r services/ app/src/ \
          --exclude '**/tests/**,**/__pycache__/**' \
          --severity-level high \
          -f txt
        
    - name: Run pip-audit (core services)
      run: |
        pip-audit -r app/requirements/profile_core_services.txt --desc
        if [ -f services/mcp-server/requirements.txt ]; then
          echo "=== Auditing MCP server ==="
          pip-audit -r services/mcp-server/requirements.txt --desc
        fi
        
    - name: Upload security reports
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: security-reports-${{ github.sha }}
        path: |
          bandit-report.json
        retention-days: 90

    - name: Comment PR with security summary
      if: github.event_name == 'pull_request'
      uses: actions/github-script@v7
      with:
        script: |
          const fs = require('fs');

          let banditIssues = 0;
          try {
            const bandit = JSON.parse(fs.readFileSync('bandit-report.json', 'utf8'));
            banditIssues = bandit.results.length;
          } catch (e) {
            banditIssues = 'N/A';
          }

          const body = `## Security Scan Results

          **Bandit Issues:** ${banditIssues}

          Download full reports from the Actions artifacts.

          ${banditIssues > 0 ? 'Please review security findings before merging.' : 'No security issues detected.'}`;

          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: body
          });
        
