# ============================================================================
# ChatBot Service - AI Assistant Project
# Python: 3.10.6
# Description: Multi-model chatbot with local LLM support and image generation
# ============================================================================

# ============= WEB FRAMEWORK =============
flask==3.1.3
flask-cors==6.0.2  # Updated from 4.0.0 - Security fix (5 vulnerabilities patched)
python-dotenv==1.2.2

# ============= FASTAPI (async mode) =============
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.27  # Required for UploadFile; >=0.0.27 fixes DoS/ArbitraryFileWrite CVEs
pydantic>=2.0.0
werkzeug>=3.0.0

# ============= PERFORMANCE & CACHING (Phase 1) =============
redis>=5.0.0
pymongo>=4.6.0
dnspython>=2.4.0  # Required for MongoDB SRV connection

# ============= RAG =============
chromadb>=0.5.0
sqlalchemy[asyncio]>=2.0.0
alembic>=1.13.0
pgvector>=0.3.0
psycopg2-binary>=2.9.12
boto3>=1.43.6
beautifulsoup4>=4.12.0
pymupdf>=1.24.0

# ============= AI APIs =============
openai>=2.29.0  # Videos API (Sora 2) requires >=2.29
google-genai>=1.56.0

# ============= IMAGE PROCESSING =============
Pillow>=12.2.0  # >=12.2.0 fixes integer overflow (CVE-2026-42311) and PDF parser DoS CVEs
requests>=2.33.1

# ============= PHASE 2: ADVANCED FEATURES =============
# Semantic search and embeddings
sentence-transformers>=2.2.2

# Scientific computing
scipy>=1.11.0
scikit-learn>=1.3.0

# ============= LOCAL MODEL SUPPORT =============
# PyTorch (CUDA 11.8) - Install from https://pytorch.org/get-started/locally/
numpy<3.0.0  # Required for PyTorch compatibility
torch>=2.6.0  # >=2.6.0 fixes heap overflow, use-after-free, and torch.load RCE CVEs
torchaudio>=2.6.0  # Keep aligned with torch version
transformers>=4.48.0  # >=4.48.0 fixes deserialization CVEs
accelerate>=1.13.0
sentencepiece>=0.1.99
protobuf>=6.33.5  # >=6.33.5 fixes multiple DoS and JSON recursion depth bypass CVEs

# Quantization (for efficient local models - Windows requires Visual C++)
bitsandbytes>=0.41.0; sys_platform != 'win32'

# ============= ADDITIONAL UTILITIES =============
# Progress bars
tqdm>=4.67.3

# JSON handling
jsonschema>=4.17.0

# Safe loading
pyyaml>=6.0.3

# File handling
aiofiles>=23.0.0

# ============= GOOGLE CLOUD / DRIVE =============
google-auth>=2.28.0
google-auth-httplib2>=0.4.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.108.0
firebase-admin>=7.3.0