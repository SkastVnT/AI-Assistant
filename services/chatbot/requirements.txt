# ============================================================================
# ChatBot Service - AI Assistant Project
# Python: 3.10.6
# Description: Multi-model chatbot with local LLM support and image generation
# ============================================================================

# ============= WEB FRAMEWORK =============
flask==3.1.3
flask-cors==6.0.5  # Updated from 4.0.0 - Security fix (5 vulnerabilities patched)
python-dotenv==1.2.2
websocket-client>=1.9.0  # ComfyUI /ws live-preview reader (Phase 3, opt-in via ANIME_PIPELINE_WS_PREVIEW)

# ============= FASTAPI (async mode) =============
fastapi>=0.139.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9  # Required for UploadFile
pydantic>=2.13.4
werkzeug>=3.0.0

# ============= PERFORMANCE & CACHING (Phase 1) =============
redis>=8.0.0
pymongo>=4.17.0
dnspython>=2.4.0  # Required for MongoDB SRV connection

# ============= RAG =============
chromadb>=1.5.9
sqlalchemy[asyncio]>=2.0.51
alembic>=1.13.0
pgvector>=0.5.0
psycopg2-binary>=2.9.12
boto3>=1.43.24
beautifulsoup4>=4.12.0
boto3>=1.43.6
beautifulsoup4>=4.15.0
pymupdf>=1.24.0

# ============= AI APIs =============
openai>=2.44.0  # Videos API (Sora 2) requires >=2.29
google-genai>=1.56.0

# ============= IMAGE PROCESSING =============
Pillow>=10.0.0
requests>=2.33.1

# ============= PHASE 2: ADVANCED FEATURES =============
# Semantic search and embeddings
sentence-transformers>=3.0.0

# Scientific computing
scipy>=1.17.1
scikit-learn>=1.9.0

# ============= LOCAL MODEL SUPPORT =============
# PyTorch (CUDA 11.8) - Install from https://pytorch.org/get-started/locally/
numpy<3.0.0  # Required for PyTorch compatibility
torch>=2.13.0
torchaudio>=2.11.0
transformers>=4.37.0
accelerate>=1.14.0
sentencepiece>=0.1.99
protobuf>=5.29.0

# Quantization (for efficient local models - Windows requires Visual C++)
bitsandbytes>=0.49.2; sys_platform != 'win32'

# ============= ADDITIONAL UTILITIES =============
# Progress bars
tqdm>=4.68.1

# JSON handling
jsonschema>=4.26.0

# Safe loading
pyyaml>=6.0.3

# File handling
aiofiles>=25.1.0

# ============= GOOGLE CLOUD / DRIVE =============
google-auth>=2.40.0
google-auth-httplib2>=0.4.0
google-auth-oauthlib>=1.4.0
google-api-python-client>=2.198.0
firebase-admin>=7.0.0