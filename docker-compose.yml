# AI-Assistant — Docker Compose (Core Services)
#
# Usage:
#   docker compose up -d                     # chatbot + mongodb only
#   docker compose --profile tools up -d     # + last30days sidecar
#   docker compose --profile hermes up -d    # + hermes agent sidecar
#   docker compose --profile all up -d       # everything
#
# Env:
#   Copy app/config/.env.example → app/config/.env and fill in API keys.
#   Copy services/chatbot/.env.example → services/chatbot/.env for service-local keys.
#
# NOTE: Image generation services (Stable Diffusion, ComfyUI) are NOT included.
#       They require GPU access and venv-image. Run them separately.

services:
  # ─── MongoDB ────────────────────────────────────────────────────────
  mongodb:
    image: mongo:7
    container_name: ai-assistant-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_ROOT_USER:-admin}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_ROOT_PASSWORD:-changeme}  # Override MONGO_ROOT_PASSWORD in .env for non-dev environments
      MONGO_INITDB_DATABASE: ai_assistant
    volumes:
      - mongodb_data:/data/db
      - ./app/docker/mongo-init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  # ─── ChatBot (primary service) ─────────────────────────────────────
  chatbot:
    build:
      context: .
      dockerfile: services/chatbot/Dockerfile
    container_name: ai-assistant-chatbot
    restart: unless-stopped
    ports:
      - "${CHATBOT_PORT:-5000}:5000"
    env_file:
      - ./app/config/.env
      - ./services/chatbot/.env
    environment:
      MONGODB_URI: mongodb://${MONGO_ROOT_USER:-admin}:${MONGO_ROOT_PASSWORD:-changeme}@mongodb:27017/ai_assistant?authSource=admin
      MONGODB_DB_NAME: ai_assistant
      FLASK_PORT: "5000"
    depends_on:
      mongodb:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import requests; requests.get('http://localhost:5000/api/v1/health', timeout=5)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    volumes:
      - chatbot_storage:/app/Storage
      - chatbot_logs:/app/logs

  # ─── last30days — Social Research Sidecar (optional) ───────────────
  # Activate with: docker compose --profile tools up -d
  last30days-setup:
    image: python:3.12-slim
    container_name: ai-assistant-last30days-setup
    profiles: ["tools", "all"]
    volumes:
      - last30days_repo:/opt/last30days
      - ./services/chatbot/vendor/last30days:/setup:ro
    command: >
      bash -c "
        if [ ! -f /opt/last30days/.cloned ]; then
          apt-get update && apt-get install -y git;
          git clone https://github.com/mvanhorn/last30days-skill.git /opt/last30days/repo;
          cd /opt/last30days/repo && pip install -r requirements.txt 2>/dev/null || true;
          touch /opt/last30days/.cloned;
          echo 'last30days setup complete';
        else
          echo 'last30days already cloned';
        fi
      "
    restart: "no"

  # ─── Hermes Agent — Advanced AI Sidecar (optional) ─────────────────
  # Activate with: docker compose --profile hermes up -d
  # Requires: HERMES_API_KEY in env, Hermes repo cloned to vendor/hermes/repo
  hermes:
    build:
      context: ./vendor/hermes
      dockerfile: Dockerfile
    container_name: ai-assistant-hermes
    profiles: ["hermes", "all"]
    restart: unless-stopped
    ports:
      - "${HERMES_PORT:-8080}:8080"
    env_file:
      - ./app/config/.env
    environment:
      HERMES_PORT: "8080"
      HERMES_API_KEY: ${HERMES_API_KEY:-}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    volumes:
      - hermes_data:/app/data

  # ─── Character Select SAA — Electron picker sidecar (optional) ─────
  # Activate with: docker compose --profile character-select up -d
  # Source: https://github.com/mirabarukaso/character_select_stand_alone_app
  # NOTE: SAA is an Electron app. Inside Docker we run only its Express +
  # WebSocket backend (browser/SAAC mode). No display server required.
  character-select:
    image: node:20-bullseye-slim
    container_name: ai-assistant-character-select
    profiles: ["character-select", "all"]
    restart: unless-stopped
    working_dir: /app
    volumes:
      - ./app/character_select_stand_alone_app-main:/app
      - character_select_node_modules:/app/node_modules
    ports:
      - "${CHARACTER_SELECT_PORT:-51028}:51028"
    environment:
      NODE_ENV: production
      SAA_WS_ADDR: "0.0.0.0"
      SAA_WS_PORT: "51028"
    command: >
      bash -c "
        if [ ! -d /app/node_modules/express ]; then
          apt-get update && apt-get install -y python3 build-essential git;
          npm install --omit=dev || npm install;
        fi;
        node ./scripts/main/saa-headless.js 2>/dev/null || node -e \"import('./webserver/back/wsService.js').then(m => m.setupHttpServer(process.cwd(), '0.0.0.0', 51028));\"
      "
    healthcheck:
      test: ["CMD", "node", "-e", "require('net').createConnection(51028,'127.0.0.1').on('connect',()=>process.exit(0)).on('error',()=>process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s

  # ─── Redis (optional, for caching/RAG) ─────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: ai-assistant-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - redis_data:/data

volumes:
  mongodb_data:
  chatbot_storage:
  chatbot_logs:
  last30days_repo:
  hermes_data:
  redis_data:
  character_select_node_modules:
