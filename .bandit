[bandit]
# Skip checks that are expected in third-party code or tests
# B101: Use of assert detected (common in tests)
# B110: Try/except with pass (common error handling pattern)
# B112: Try/except with continue (common error handling pattern)
skips = B101,B110,B112

# Note: exclude_dirs is not used in this config because the workflow
# explicitly scans only ChatBot and Speech2Text Services/app/core directories.
# If you need to exclude subdirectories within those paths, add them here:
# exclude_dirs = ChatBot/tests,ChatBot/venv_chatbot
